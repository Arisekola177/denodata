
export const metadata = {
    title: "Contact us || Denodata",
    description: "Supermarket Business Solutions",
  };

  export default function ContactLayout({
    children,
  }: Readonly<{
    children: React.ReactNode;
  }>) {
  return (
    <div>
       {children}
    </div>
  )
}

